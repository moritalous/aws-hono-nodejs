{
  "name": "backend",
  "version": "0.0.1",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/lambda.ts --target node --outdir dist --minify"
  },
  "dependencies": {
    "@ai-sdk/amazon-bedrock": "^4.0.96",
    "@aws-sdk/credential-providers": "^3.1032.0",
    "@hono/node-server": "^1.19.14",
    "ai": "^6.0.168",
    "hono": "^4.12.14"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
